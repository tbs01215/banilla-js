console.dir(document)

이걸 통해 document.#$#$#% 뭐가 올 수 있는지 알 수 있다

console.dir(title) # const title = document.getElementById("title")

얘도 마찬가지임

addEventListener("여기 있는 이벤트를 받으면", 이 함수 실행해)

```
const title = document.querySelector("#title")

function northColor (event) {
  title.style.color = 'red'
}

title.addEventListener("click", northColor)
```
